"""Manual image links used by the saved-team cards.

Use normalized character IDs as keys. Empty values are allowed when only one
of the two images should be overridden.
"""

TEAM_SAVED_IMAGE_URLS: dict[str, dict[str, str]] = {
    # "aalto": {
    #     "char": "https://example.com/aalto-character.png",
    #     "weapon": "https://example.com/aalto-weapon.png",
    # },
}
